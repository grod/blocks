{
  "name": "@grod/blocks",
  "version": "0.0.1",
  "bin": "blocks.js",
  "scripts": {
    "start": "run-p watch:blocks dev:svite",
    "dev": "run-p watch:blocks dev:svite",
    "dev:svite": "svite",
    "watch:blocks": "node blocks.js",
    "build": "run-s build:routify build:svite",
    "build:svite": "svite build"
  },
  "dependencies": {
    "svelte": "^3.23.2"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "postcss-import": "^12.0.1",
    "postcss-load-config": "^2.1.0",
    "postcss-preset-env": "^6.7.0",
    "svelte-preprocess": "^3.9.10",
    "svite": "^0.2.3",
    "tailwindcss": "^1.4.6",
    "vite": "1.0.0-beta.11"
  }
}
